script.
    // Navigation.pug
    let views = []
    let currentView = ""

nav.navbar.navbar-inverse.navbar-fixed-top
        .container
            .navbar-header
                button(
                    type="button"
                    class="navbar-toggle collapsed"
                    data-toggle="collapse"
                    data-target="#navbar"
                    aria-expanded="false"
                    aria-controls="navbar"
                )
                    span.sr-only Toggle Navigation
                    span.icon-bar
                    span.icon-bar
                    span.icon-bar
                a(
                    class="navbar-brand"
                    href="/"
                ) Free Langer
                    small v0.1
            #navbar.collapse.navbar-collapse
                ul.nav.navbar-nav
                    li( class="{ currentView == views[0] ? 'active' : ''}")
                        a(href="/") Home

                    li( class="{ currentView == 'views[1]' ? 'active' : ''}")
                        a(href="/learn" target="_self") Learn

style.
    .navbar-brand small {
        font-size: 0.5em;
        margin-left: 5px;
    }